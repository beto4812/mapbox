"# mapbox"

Use npm to install dependencies:

```
npm install
```

Use npm to install bower:

```
npm install -g bower
```

Install bower dependencies:

```
bower install --save
```

Serve project:

```
gulp serve
```
